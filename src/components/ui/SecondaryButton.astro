---
interface Props {
  text: string;
  href?: string;
  type?: 'button' | 'submit' | 'reset';
  className?: string;
}

const { text, href, type = 'button', className = '' } = Astro.props;
---
{href ? (
  <a href={href} class={`btn-secondary ${className}`}>
    {text}
  </a>
) : (
  <button type={type} class={`btn-secondary ${className}`}>
    {text}
  </button>
)}

<style>
  .btn-secondary {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    background-color: transparent;
    color: var(--color-bright-blue);
    padding: 14px 28px;
    border-radius: var(--radius-button);
    font-family: 'Inter', sans-serif;
    font-size: 16px;
    font-weight: 600;
    text-decoration: none;
    border: 1px solid var(--color-bright-blue);
    cursor: pointer;
    transition: background-color 200ms ease;
  }

  .btn-secondary:hover {
    background-color: var(--color-light-blue);
  }
</style>
